# 律师本地匿名化助手操作教程

## 1. 这是什么

这是一个只在本地电脑运行的小工具。客户文件先在本地替换成代号，再上传给 Kimi、ChatGPT 等 AI 工具；AI 处理后，工具再在本地把代号还原。

示例：

```text
张三 -> [[PERSON_001]]
上海某某科技有限公司 -> [[COMPANY_001]]
```

AI 看到的是代号，不直接看到真实客户信息。

## 2. 怎么打开

解压 `LegalAnonymizer.zip` 后，双击：

```text
LegalAnonymizer.exe
```

不需要配置账号，不需要联网，不需要写代码。

如果需要重新下载最新版，请打开：

```text
https://github.com/ShengrenHOU/lawyer-local-anonymizer/releases/latest
```

只下载 `LegalAnonymizer.zip`。不要下载 `Source code`，也不要点绿色 `Code` 按钮。

如果已经装过旧版本，下载新版 zip 后解压到新文件夹，再双击新的 `LegalAnonymizer.exe` 即可。原来的工作文件夹可以继续保留，不要删除 `99-本地映射表-不要上传`。

## 3. 只需要记住 3 步

### 第一步：放原文件

点击程序里的：

```text
放原文件
```

把客户的 Word、可复制 PDF、txt 或 md 文件放进去。

如果是旧版 `.doc` 文件，程序会尝试本地转换；如果处理失败，请先用 Word 或 WPS 打开，再另存为 `.docx` 后重新放入。

### 第二步：拿去上传 AI

等程序提示处理完成后，点击：

```text
拿去上传AI
```

只上传这里面的匿名化文件给 Kimi / ChatGPT。

如果原文件是 `.docx`，匿名化文件也会是 `.docx`，会尽量保留原 Word 的段落、表格、页眉页脚和字体。

同时可以点击：

```text
复制AI提示词
```

把提示词粘贴给 AI。提示词会要求 AI 不要改动 `[[PERSON_001]]` 这类代号。

### 第三步：还原 AI 结果

如果 AI 可以下载结果文件：

1. 点击 `放AI结果`
2. 把下载文件放进去
3. 程序会自动还原
4. 点击 `看还原结果`

如果 AI 不能下载，只能复制网页回复：

1. 把 AI 回复复制到程序窗口的大文本框
2. 点击 `粘贴AI回复并还原`
3. 点击 `看还原结果`

通常不需要手动选择映射表。只有程序提示无法自动还原时，再点击 `手动选择映射表还原`。

## 4. 哪些文件可以上传给 AI

只上传这个文件夹里的文件：

```text
02-已匿名化-可上传AI
```

如果文件进入下面这个文件夹，先不要上传：

```text
02-需要复核-暂勿上传
```

这说明程序发现可能还有未脱敏内容、Word 里有图片/嵌入对象等不确定结构，或者 AI 结果破坏了代号。

## 5. 绝对不要上传什么

不要上传这个文件夹：

```text
99-本地映射表-不要上传
```

这里保存真实信息和代号的对应表，是还原所必需的本地资料。

## 6. 文件夹不用清理

可以长期保留文件夹里的历史文件。程序会记录哪些文件已经处理过，不会每次启动都重复处理。

如果文件内容改变，程序会重新处理。

## 7. 本地学习记忆

程序会把已经处理过的姓名、公司名、地址等内容记在本机本地记忆里。以后再遇到相同内容，会更容易自动识别。

如果您发现某个客户名、公司名、项目名、英文简称没有被处理，可以在主界面输入这个词，点击：

```text
加入一定脱敏
```

以后再遇到这个词，程序会自动替换。

如果某个普通法律词或模板词被误处理，可以输入这个词，点击：

```text
加入一定不脱敏
```

以后再遇到这个词，程序会尽量保留。为了安全，如果保留后仍然像手机号、身份证号、真实地址等高风险信息，程序仍会把文件放入“需要复核”。

这个记忆只保存在本地电脑。如果不想继续保留，可以点击：

```text
清空本地学习记忆
```

清空学习记忆不会删除已经生成的脱敏对照表，也不会影响已经处理过的文件还原。

还可以点击：

```text
本地规则
历史项目
```

`本地规则` 用来查看当前记住了哪些“一定脱敏 / 一定不脱敏”的词。  
`历史项目` 用来查看最近处理过哪些文件、是否通过、是否需要复核。

## 8. 支持和不支持

目前支持：

```text
Word: .docx
旧版 Word: .doc，需要本机 Microsoft Word 支持自动转换
可复制 PDF: .pdf
纯文本: .txt
Markdown: .md
```

目前不支持：

```text
扫描 PDF
图片
手写内容
盖章图片里的文字
加密 PDF
```

## 9. 一句话流程

```text
放原文件 -> 拿匿名化文件上传 AI -> AI 结果放回或粘贴 -> 看已还原结果
```
