# 叶律师操作教程：本地匿名化助手

## 1. 这是什么

这是一个只在本地电脑运行的小工具。客户文件先在本地替换成代号，再上传给 Kimi、ChatGPT 等 AI 工具；AI 处理后，工具再在本地把代号还原。

例子：

```text
张三 -> [[PERSON_001]]
上海某某科技有限公司 -> [[COMPANY_001]]
```

AI 只看到代号，看不到真实客户信息。

## 2. 怎么打开

解压 `LegalAnonymizer.zip` 后，双击：

```text
LegalAnonymizer.exe
```

打开后不用配置账号，不用联网，不用写代码。

## 3. 只需要记住 3 步

### 第一步：放入客户文件

点击：

```text
打开待匿名化文件夹
```

把客户的 Word、可复制 PDF、txt 或 md 文件放进去。

### 第二步：上传匿名化文件给 AI

等程序提示“已生成可上传 AI 文件”后，点击：

```text
打开可上传AI文件夹
```

只上传这里面的匿名化文件给 Kimi / ChatGPT。

同时可以点击：

```text
复制AI提示词
```

然后把提示词粘贴给 AI。这个提示词会要求 AI 不要改动 `[[PERSON_001]]` 这样的代号。

### 第三步：还原 AI 结果

如果 AI 可以下载结果文件：

1. 点击“打开AI结果待还原文件夹”。
2. 把下载文件放进去。
3. 程序会自动还原。
4. 点击“查看最近处理结果”。

如果 AI 不能下载，只能复制网页回复：

1. 把 AI 回复复制到程序窗口的大文本框。
2. 点击“粘贴AI回复并还原”。
3. 点击“查看最近处理结果”。

通常不需要手动选择映射表。只有程序提示无法自动还原时，再点：

```text
手动选择映射表还原
```

## 4. 程序会创建哪些文件夹

第一次运行后，程序会自动创建：

```text
律师本地匿名化助手/
  01-待匿名化/
  02-已匿名化-可上传AI/
  02-需要复核-暂勿上传/
  03-AI结果文件-待还原/
  04-已还原/
  99-本地映射表-不要上传/
```

每个文件夹的用途：

```text
01-待匿名化
放客户原始文件。

02-已匿名化-可上传AI
只上传这里面的文件给 AI。这里面的文件通过了自动漏扫。

02-需要复核-暂勿上传
程序发现可能还有未脱敏内容。这里面的文件不要上传给 AI。

03-AI结果文件-待还原
AI 下载结果放这里。

04-已还原
看最终还原结果。

99-本地映射表-不要上传
这里保存真实信息和代号的对照表，绝对不要上传给 AI。
```

## 5. 绝对不要上传的内容

不要上传这个文件夹：

```text
99-本地映射表-不要上传
```

这个文件夹里有：

```text
.mapping.json
脱敏信息对照表.xlsx
report.txt
```

其中 `.mapping.json` 和 `脱敏信息对照表.xlsx` 都可能包含真实客户信息。

## 6. 支持和不支持的文件

目前支持：

```text
Word: .docx
可复制 PDF: .pdf
纯文本: .txt
Markdown: .md
```

目前不支持：

```text
扫描 PDF
图片
手写内容
盖章图片里的文字
加密 PDF
```

如果 PDF 里的文字不能复制，这一版一般也识别不了。

## 7. 使用时的安全习惯

上传 AI 前，快速打开匿名化后的文件看一眼：

```text
02-已匿名化-可上传AI
```

确认里面没有明显的真实姓名、公司名、手机号、地址后再上传。

如果发现漏掉了敏感信息，先不要上传，把情况反馈给开发者补规则。

如果文件进入“02-需要复核-暂勿上传”，说明程序已经自动拦截，不要上传该文件。

## 8. 一句话版流程

```text
放原文件 -> 拿匿名化文件上传 AI -> AI 结果放回或粘贴 -> 查看已还原结果
```
